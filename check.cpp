#include "check.h" 
float check() {
	float value;
	while (true) {
		cin >> value;
		if (cin.fail()) {
			cin.clear();
			cin.ignore(numeric_limits<streamsize>::max(),
				'\n');
		число: "; 
			'\n');
		}
	}
	Файл figur.h:
	cout << "Некорректный ввод! Пожалуйста, введите 
}
else {
	cin.ignore(numeric_limits<streamsize>::max(),
		return value;
}